# ── Base de datos PostgreSQL ──────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=inventario
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# ── Servidor ──────────────────────────────────────────────
PORT=3001
# En produccion cambia a 0.0.0.0 para escuchar en toda la red
HOST=127.0.0.1
# Origenes permitidos para CORS (separados por coma)
CORS_ORIGIN=http://localhost:5173

# ── Autenticacion ─────────────────────────────────────────
JWT_SECRET=cambia_esto_por_una_clave_secreta_larga
APP_USER=admin
APP_PASSWORD=cambia_esto_por_un_password_propio
